<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Dictionaries/Colours.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="BaseListViewStyle"
           TargetType="{x:Type ListBox}">
        <Setter Property="Background"
                Value="Transparent" />
        <Setter Property="BorderBrush"
                Value="Transparent" />
        <Setter Property="HorizontalContentAlignment"
                Value="Stretch" />
        <Setter Property="SelectionMode"
                Value="Single" />
        <Setter Property="VerticalAlignment"
                Value="Stretch" />
        <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBox}">
                        <Border Grid.Row="2"
                                CornerRadius="5"
                                BorderThickness="1"
                                BorderBrush="{StaticResource AccentDarkOver}"
                                Background="{TemplateBinding Background}"
                                Margin="0 5">
                            <ItemsPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
    </Style>

    <Style x:Key="MainListViewStyle"
           TargetType="{x:Type ListBox}"
           BasedOn="{StaticResource BaseListViewStyle}">
    </Style>

    <Style x:Key="SubTasksListStyle"
           TargetType="{x:Type ListBox}"
           BasedOn="{StaticResource BaseListViewStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding SelectedTask}"
                         Value="{x:Null}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ListBox}">
                            <Border Grid.Row="2"
                                    CornerRadius="5"
                                    BorderThickness="0"
                                    Margin="0 5">
                                <ItemsPresenter />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="{x:Type ListBoxItem}"
           x:Key="MainListViewItemStyle">
        <Setter Property="Cursor"
                Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                    <Grid HorizontalAlignment="Stretch">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="auto" />
                        </Grid.RowDefinitions>
                        <Border Background="{TemplateBinding Background}"
                                VerticalAlignment="Stretch"
                                HorizontalAlignment="Stretch"
                                CornerRadius="4"
                                Margin="3">
                            <ContentPresenter />
                        </Border>
                        <Separator Grid.Row="1"
                                   Margin="3 0" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver"
                     Value="True">
                <Setter Property="Background"
                        Value="SandyBrown" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="SubListTaskItem"
           TargetType="{x:Type ListBoxItem}">
        <Setter Property="Background"
                Value="LightGray" />
    </Style>
</ResourceDictionary>